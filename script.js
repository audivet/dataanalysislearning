document.addEventListener('DOMContentLoaded', function() {
    const container = document.getElementById('learning-plan');
    container.innerHTML = "<p>Hello, this is a test to see if the script is running.</p>";
});
